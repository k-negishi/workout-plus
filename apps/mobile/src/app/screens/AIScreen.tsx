/**
 * AI タブ画面（プレースホルダー）
 * 後のフェーズで AI チャット機能を実装する
 */
import React from 'react';

import { PlaceholderScreen } from './PlaceholderScreen';

export function AIScreen() {
  return <PlaceholderScreen name="AI" />;
}
